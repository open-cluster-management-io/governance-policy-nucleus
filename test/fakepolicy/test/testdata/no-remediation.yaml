apiVersion: policy.open-cluster-management.io/v1beta1
kind: FakePolicy
metadata:
  name: fakepolicy-sample
  namespace: default
spec:
  severity: low
  namespaceSelector:
    include: ["*"]
    exclude: ["kube-*"]
